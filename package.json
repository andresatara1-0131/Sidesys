{
  "name": "suite-playwright",
  "private": true,
  "scripts": {
    "test": "cross-env NODE_OPTIONS=\"-r tsconfig-paths/register\" npx playwright test --project=chromium",
    "test:all": "cross-env NODE_OPTIONS=\"-r tsconfig-paths/register\" npx playwright test",
    "test:ui": "cross-env NODE_OPTIONS=\"-r tsconfig-paths/register\" npx playwright test --ui",
    "report": "npx playwright show-report",
    "pw:install": "npx playwright install --with-deps",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.6.2"
  }
}